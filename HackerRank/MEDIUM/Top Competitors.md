```sql
/*
Enter your query here.
Please append a semicolon ";" at the end of the query and enter your query in a single line to avoid error.
*/

SELECT H.HACKER_ID, 
       H.NAME,
       COUNT(C.CHALLENGE_ID) AS AMOUNT_CHALLENGE,
       D.DIFFICULTY_LEVEL,
       MAX(S.SCORE) AS SCORE
FROM HACKERS H
JOIN 
    CHALLENGES C ON H.HACKER_ID = C.HACKER_ID
JOIN 
    DIFFICULTY D ON C.DIFFICULTY_LEVEL = D.DIFFICULTY_LEVEL
JOIN 
    SUBMISSIONS S ON H.HACKER_ID = S.HACKER_ID
 
GROUP BY H.HACKER_ID, H.NAME,D.DIFFICULTY_LEVEL
HAVING COUNT( C.CHALLENGE_ID) > 1 AND MAX(S.SCORE) 

ORDER BY  D.DIFFICULTY_LEVEL ASC--, S.SCORE DESC

/*
SELECT H.HACKER_ID, 
       H.NAME,
       C.CHALLENGE_ID, 
       D.DIFFICULTY_LEVEL,
       S.SCORE
FROM HACKERS H
JOIN 
    CHALLENGES C ON H.HACKER_ID = C.HACKER_ID
JOIN 
    DIFFICULTY D ON C.DIFFICULTY_LEVEL = D.DIFFICULTY_LEVEL
JOIN 
    SUBMISSIONS S ON H.HACKER_ID = S.HACKER_ID
ORDER BY  H.HACKER_ID, D.DIFFICULTY_LEVEL ASC, S.SCORE DESC
*/

```
